package com.otus.selenium;

import org.junit.Test;
import org.openqa.selenium.WebDriver;

public class LocatorsTest {
    @Test
    public void StartTest(){

        //Lesson3 test = new Lesson3();
        /*
        test.HomepageOpening();
        test.Login("tester","tester");
        test.SelectTestsEditTree();
        String tsname = test.CreateTestSuite("This is a new TestSuite #"+ test.now(),"Autotest suite");
        test.SelectTestSuite(tsname);
        String tcname = test.CreateTestCase("Testcase #1 #"+test.now(),
                                            "Описание теста: цель, сценарий и исходное состояние программы",
                                            "Условия");
        test.SelectTestCase(tsname,tcname);
        test.SelectStepAdding();
        test.AddTestStep("TC 1 Step 1","Step 1 in TC 1 was added");
        test.AddTestStep("TC 1 Step 2","Step 2 in TC 1 was added");
        test.AddTestStep("TC 1 Step 3","Step 3 in TC 1 was added");
        test.SelectTestSuite(tsname);
        tcname = test.CreateTestCase("Testcase #2 #"+test.now(),
                "Описание теста: цель, сценарий и исходное состояние программы",
                "Условия");
        test.SelectTestCase(tsname,tcname);
        test.SelectStepAdding();
        test.AddTestStep("TC 2 Step 1","Step 1 in TC 2 was added");
        test.AddTestStep("TC 2 Step 2","Step 2 in TC 2 was added");
        test.AddTestStep("TC 2 Step 3","Step 3 in TC 2 was added");
         */
    }
}
